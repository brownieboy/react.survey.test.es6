{
  "name": "react.survey.test",
  "version": "0.0.1",
  "description": "A survey of checkbox questions and answers generated by ReactJS, ES6 version",
  "repository": "https://github.com/brownieboy/react.survey.test",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "TARGET=build webpack",
    "buildwin": "SET TARGET=build&& webpack",
    "start": "TARGET=dev webpack-dev-server --devtool eval --progress --colors --hot --inline --history-api-fallback",
    "startwin": "SET TARGET=dev&& webpack-dev-server --devtool eval --progress --colors --hot --inline --history-api-fallback"
  },
  "author": "Michael Brown",
  "license": "ISC",
  "devDependencies": {
    "babel-core": "^5.8.3",
    "babel-loader": "^5.3.2",
    "path": "^0.11.14",
    "webpack": "^1.10.3"
  }
}
